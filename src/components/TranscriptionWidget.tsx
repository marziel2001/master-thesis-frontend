export default function TranscriptionWidget({ model }) {
    return (
        <div className="bg-gray-200 p-4 rounded-lg text-black m-2">
            Transcription Widget ({model})
        </div>
    )
}
